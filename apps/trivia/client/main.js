const root = document.getElementById('app');
root.innerHTML = `
  <div style="font-family: system-ui, sans-serif; max-width: 720px; margin: 2rem auto; padding: 1rem;">
    <h1>Trivia (stub)</h1>
    <p>This placeholder confirms the monorepo can deploy a second game frontend under <code>/g/trivia/</code>.</p>
  </div>
`;
